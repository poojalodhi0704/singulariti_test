import { Route } from '@angular/router';
import { PlayerListComponent } from './index';

export const PlayerListRoutes: Route[] = [
      {
        path: '',
        component: PlayerListComponent
      }
];
